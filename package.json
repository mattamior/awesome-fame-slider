{
  "name": "awesome-fame-slider",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && node scripts/generate-share-cards.mjs && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "cf:dev": "wrangler dev",
    "cf:types": "wrangler types",
    "cf:deploy": "npm run build && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply awesome-fame-slider-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply awesome-fame-slider-db --remote"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "sharp": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  }
}